apiVersion: v1
kind: Service
metadata:
  name: {{ include "production-master.fullname" . }}
  labels:
    {{- include "production-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "production-master.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
