apiVersion: v1
kind: Secret
metadata:
  name: {{ include "production-master.fullname" . }}-secret
  labels:
    {{- include "production-master.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64-encoded values — inject via CI/CD or sealed-secrets
  POSTGRES_PASSWORD: ""
  ANTHROPIC_API_KEY: ""
  JWT_SECRET: ""
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
